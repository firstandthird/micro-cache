verbose: true
origin: '{{ENV.CACHE_ORIGIN}}'
secret: '{{ENV.CACHE_SECRET}}'
redis:
  host: '{{ENV.REDIS_HOST}}'
  port: '{{ENV.REDIS_PORT}}'
  password: '{{ENV.REDIS_PASSWORD}}'
  prefix: 'prefix'
plugins:
  hapi-auto-handler:
    _priority: -5
  './plugins/store':
